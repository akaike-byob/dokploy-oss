/**
 * Upstream uses this to push whitelabeling config (title, favicon, colours) into the document.
 * This fork ships stock branding, so there is nothing to apply.
 */
export const WhitelabelingProvider = () => null;
