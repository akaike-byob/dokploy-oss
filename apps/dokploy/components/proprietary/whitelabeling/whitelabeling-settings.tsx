import { FeatureUnavailable } from "../feature-unavailable";

export const WhitelabelingSettings = () => (
	<FeatureUnavailable title="Whitelabeling">
		Custom logos, colours, CSS and error pages are not part of this fork. The
		panel keeps its default appearance.
	</FeatureUnavailable>
);
